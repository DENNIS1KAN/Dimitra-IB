# M11 + M12: the IA and UI rebuild (2026-08-24)

Owner directive: rebuild the admin per `design/admin-blend-final.html` (M11) and the
student app per `design/student-direction-2-course-hub.html` (M12). IA and UI
reorganization only: no schema changes, no gating changes, existing server actions
reused; moved routes get redirects. Decisions recorded in SPEC §15.7 #23/#24; the
verify checklists live in SPEC §15.6. No em or en dashes in any string.

## M11 (admin) task order

1. `lib/content.ts` + tests: completeness labels, missing types, nudge line, ready
   summary (the one place the rail, the checklist and the home nudge agree).
2. Server-action rework in `app/admin/actions.ts`: a validated `back` redirect field
   for enrollment/student actions (strip, drawer and course tab all host the same
   forms), `createCohort` → the new course page, `updateCohort` gains subject /
   level / exam year, `createModule` errors return to the course composer, new
   `saveModuleNote` (autosave), `reorderMaterials` (drag order) and `removeMaterial`
   actions,
   `replaceId` on the materials upload route.
3. `/admin` home (Needs-you strip + course cards), `/admin/courses/new`,
   `/admin/courses/[id]` tabs (Modules / Students / Progress / Details, `?tab=`),
   progress matrix extracted to a per-cohort component.
4. Week editor rebuilt at `/admin/courses/[id]/weeks/[moduleId]` (four slots,
   checklist, note autosave, week-details card); `/admin/modules/[id]` becomes a
   lookup redirect; preview at `/admin/preview/modules/[id]`.
5. `/admin/students` rows + drawer (`?open=` searchParam sheet) + dashed-row create
   form.
6. Nav down to five items; retire `/admin/modules`, `/admin/requests`,
   `/admin/progress`, `/admin/courses` (list) as redirects.
7. DESIGN.md §6 admin recipes updated; verify per SPEC §15.6 M11; commit, push.

## M12 (student) task order — only after M11 is pushed

1. `lib/queries.ts`: extend `studentCourses` with per-course totals + current
   module; add `studentCourseDetail` (active enrollment only, ascending rail).
2. `/app` = My courses + catalog (promoted from `/app/courses`); every active card
   Continue deep-links to that course's current module (orange only on the overall
   current course, SPEC §15.7 #24); paused cards keep the paused copy.
3. `/app/courses/[id]` course hub: note card on top, course-scoped ascending rail.
4. `/app/schedule` = calendar + clinic line + "Book a 1:1 on Google Meet".
5. Nav: Courses / Messages / Schedule; Account behind the initials chip.
6. Redirects: `/app/assignments` → `/app`, `/app/courses` → `/app`,
   `/app/sessions` and `/app/calendar` → `/app/schedule`. Drop dead code
   (`studentAssignments`) with its tests.
7. DESIGN.md §6 student recipes updated; verify per SPEC §15.6 M12; commit, push.

## Standing constraints

- One orange CTA per view (SPEC §15.7 #13); green text is Forest; tints are alpha
  of brand hexes.
- Mobile first: 390px before 1280px on every changed screen.
- Rule 3 short-circuits in the /app layout and must survive untouched.
- `lib/gating.ts` untouched; Rule 1 filtering stays in `lib/queries.ts`.
